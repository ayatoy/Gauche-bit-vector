# Succinct bit vector for Gauche

## Install

    $ git clone git://github.com/ayatoy/Gauche-bit-vector.git
    $ cd Gauche-bit-vector
    $ ./DIST gen
    $ ./configure
    $ make
    $ make check
    $ [sudo] make install
